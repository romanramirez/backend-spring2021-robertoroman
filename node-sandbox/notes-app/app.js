// // // The require function allows us to load in node modules.
// // // Node.js follows the CommonJS module system.
const notes = require('./notes.js');
const validator = require('validator');
const chalk = require('chalk');

// // This, like the one above, assigns our require function to our add variable.
// const add = require('./utils');

// // This pulls from our add variable, which holds the data from our ./ utils file, which has a function to add two numbers together.
// const sum = add(4, 3)

// We are using process. to access the global object in Node.
console.log();
